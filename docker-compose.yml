version: "3.9"

services:
  app:
    image: team-app:dev
    build:
      context: .
      dockerfile: Dockerfile
    command: ["sh", "-c", "echo 'Добавьте сервисы и команды запуска в docker-compose.yml' && sleep 3600"]
